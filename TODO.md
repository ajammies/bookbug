# TODO

## Pipeline

- [ ] Abstract `stopAfter` into a dynamic step registry instead of hardcoded union type (`'brief' | 'manuscript' | 'story' | 'book'`). This avoids updating the type every time a new step is added.
